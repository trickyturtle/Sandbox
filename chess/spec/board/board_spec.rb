
require "spec_helper"

describe Board do
  describe ".read_moves" do
    context "given a .txt of moves" do
      it "returns an array of movement pairs" do
        expect(StringCalculator.add("")).to eql(0)
      end
    end
end
